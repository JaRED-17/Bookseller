var $booksContainer = $('#content .container #books'),
    $searchbar = $('#content .container #searchbar'),
    $sliderContainer = $('#content .container #slider');

appendComponent('slider', $sliderContainer);
appendComponent('searchbar', $searchbar);
appendComponent('books', $booksContainer);